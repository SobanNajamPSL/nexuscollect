# Narration — recording sheets

Generated by `npx tsx scripts/record-demo.ts --dry --manifest`. Do not edit by hand —
the script lives in `scripts/record-demo.ts` beside the actions it narrates, so that a
line and the thing it describes cannot drift apart. Change it there and regenerate.

The film has **no on-screen text**. The narration carries the entire explanation.

## How to record

One file per beat is easiest — read the numbered lines in order, leaving a clear
**one to two second pause** between them. The pauses are how the pipeline finds
where each line ends, so they matter more than they look like they should.

```
docs/demo/narration/audio/<beat-id>.wav        one take for the whole beat
docs/demo/narration/audio/<beat-id>/01.wav    or one file per line, if you prefer
```

WAV or AIFF, mono is fine, 44.1 kHz or better.

### Using a text-to-speech tool

`text/` holds the same words with no Markdown, because a synthesiser would otherwise
say the asterisks:

```
text/<beat>/01.txt    one line per file — feed these in, save the output as
                      audio/<beat>/01.wav and the names already line up
text/<beat>.txt       the whole beat in one file, passages separated by blank lines
```

Per-line files are the better path for TTS: `measure-narration` prefers them and skips
silence detection entirely, so there is nothing to tune and no chance of a boundary
landing in the wrong place.

Then:

```bash
npx tsx scripts/measure-narration.ts
```

which splits any whole-beat takes, checks it found the right number of lines, and
writes `durations.json`. After that, re-record the film and the beats as usual and
every caption is held to the length of its narration instead of an estimate.

## Beats

| Beat | Lines | Words | Est. spoken |
|---|---|---|---|
| [00-cold-open](00-cold-open.md) — Cold open — an agency's own position | 4 | 277 | ~1m 41s |
| [01-request-to-pay](01-request-to-pay.md) — The agency asks to be paid | 5 | 131 | ~0m 48s |
| [02-request-fulfilled](02-request-fulfilled.md) — The money arrives, and the request closes itself | 3 | 95 | ~0m 35s |
| [03-citizen-pays](03-citizen-pays.md) — A citizen pays bills across two agencies | 10 | 361 | ~2m 11s |
| [04-counter](04-counter.md) — A teller takes cash and lodges a cheque | 9 | 300 | ~1m 49s |
| [05-agency-position](05-agency-position.md) — The agency's position has moved | 2 | 85 | ~0m 31s |
| [06-reconcile](06-reconcile.md) — Reconciling the day, under maker-checker | 8 | 274 | ~1m 40s |
| [07-cheque-bounces](07-cheque-bounces.md) — The cheque bounces, and six things unwind | 3 | 136 | ~0m 49s |
| [08-prove-it](08-prove-it.md) — Prove it: five assertions, a tamper, and the scroll | 8 | 267 | ~1m 37s |
